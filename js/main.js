/**
 * Main JS for website.
 */

/**
 * @name expandDong
 * @description Expands an element, so you can read more.
 * @param elem The element to be expanded.
 */
function expandDong(elem) {
    
}

/**
 * @name collapseElement
 * @description Collapses the element.
 * @param elem The element to be collapsed.
 */
function collapseElement(elem) {
    
}